<UserControl
    x:Class="BaconographyW8.View.CommentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BaconographyW8.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <DataTemplate x:Key="Type:CommentViewModel">
            <local:CommentView/>
        </DataTemplate>
        <DataTemplate x:Key="Type:MoreViewModel">
            <local:MoreCommentsView/>
        </DataTemplate>
    </UserControl.Resources>


    <Grid Background="{Binding Path=OddNesting, Converter={StaticResource alternatingColorConverter}}" Margin="5">        
        <Grid Visibility="{Binding IsMinimized, Converter={StaticResource booleanVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource TextButtonStyle}" VerticalAlignment="Top"  Command="{Binding MaximizeCommand}" CommandParameter="{Binding}">[+]</Button>
                <TextBlock Text="{Binding Path=Votable.TotalVotes}" Margin="5,0,0,0" />
                <TextBlock Text="points" Margin="5,0,0,0" />
                <TextBlock Text="{Binding CreatedUTC, Converter={StaticResource timeRelationConverter}}" Margin="5,0,0,0" />
                <TextBlock Text="by" Margin="5,0,0,0"/>
                <Button Style="{StaticResource TextButtonStyle}" Margin="5,0,0,0" VerticalAlignment="Top" CommandParameter="{Binding PosterName}" Command="{Binding GotoUserDetails, Source={StaticResource StaticCommands}}">
                    <Button.Content>
                        <Border Margin="0,0,0,0" BorderThickness="0" Background="{Binding PosterFlair, Converter={StaticResource authorFlairKindConverter}}">
                            <TextBlock Margin="0,0,0,0" Text="{Binding PosterName}" />
                        </Border>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>
        <Grid Visibility="{Binding IsMinimized, Converter={StaticResource invertBooleanVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">
                <ToggleButton Command="{Binding Path=Votable.ToggleUpvote}" CommandParameter="{Binding Votable}" IsChecked="{Binding Path=Votable.Like, Mode=TwoWay}" FontFamily="Marlett" FontSize="20" Content="5" Padding="0" BorderThickness="0" />
                <ToggleButton Command="{Binding Path=Votable.ToggleDownvote}" CommandParameter="{Binding Votable}" IsChecked="{Binding Path=Votable.Dislike, Mode=TwoWay}"  FontFamily="Marlett" FontSize="20" Content="6" Padding="0" BorderThickness="0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource TextButtonStyle}" VerticalAlignment="Top"  Command="{Binding MinimizeCommand}" CommandParameter="{Binding}">[-]</Button>
                    <TextBlock Text="{Binding Path=Votable.TotalVotes}" Margin="5,0,0,0" />
                    <TextBlock Text="points" Margin="5,0,0,0" />
                    <TextBlock Text="{Binding CreatedUTC, Converter={StaticResource timeRelationConverter}}" Margin="5,0,0,0" />
                    <TextBlock Text="by" Margin="5,0,0,0"/>
                    <Button Style="{StaticResource TextButtonStyle}" Margin="5,0,0,0" VerticalAlignment="Top" CommandParameter="{Binding PosterName}" Command="{Binding GotoUserDetails, Source={StaticResource StaticCommands}}">
                        <Button.Content>
                            <Border Margin="0,0,0,0" BorderThickness="0" Background="{Binding PosterFlair, Converter={StaticResource authorFlairKindConverter}}">
                                <TextBlock Margin="0,0,0,0" Text="{Binding PosterName}" />
                            </Border>
                        </Button.Content>
                    </Button>
                </StackPanel>
                <ContentPresenter Content="{Binding Body, Converter={StaticResource markdownConverter}}" HorizontalAlignment="Stretch"/>
                <StackPanel Orientation="Horizontal">
                    <Button BorderThickness="0" Margin="0,0,0,0" Command="{Binding GotoContext}" CommandParameter="{Binding}">parent</Button>
                    <Button BorderThickness="0" Margin="0,0,0,0" Command="{Binding Report}" CommandParameter="{Binding}">report</Button>
                    <Button BorderThickness="0" Margin="0,0,0,0" Command="{Binding Save}" CommandParameter="{Binding}">save</Button>
                    <Button BorderThickness="0" Margin="0,0,0,0" Command="{Binding GotoReply}" CommandParameter="{Binding}">reply</Button>
                </StackPanel>
                <ContentControl Content="{Binding ReplyData, Converter={StaticResource replyConverter}}"></ContentControl>
                <ItemsControl ItemsSource="{Binding Path=Replies}" HorizontalAlignment="Stretch" 
                              HorizontalContentAlignment="Stretch" ItemTemplateSelector="{StaticResource typedTemplateSelector}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Template>
                        <ControlTemplate>
                            <ItemsPresenter />
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
