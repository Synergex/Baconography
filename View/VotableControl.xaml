<UserControl
    x:Class="Baconography.View.VotableControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Baconography.View"
    xmlns:vm="using:Baconography.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <StackPanel Grid.Column="0" Orientation="Vertical">
        <ToggleButton  Command="{Binding ToggleUpvote}" IsChecked="{Binding Like, Mode=TwoWay}" FontFamily="Marlett" FontSize="20" Content="5" Padding="0" BorderThickness="0" />
        <TextBlock Text="{Binding Path=TotalVotes}" />
        <ToggleButton Command="{Binding ToggleDownvote}" IsChecked="{Binding Dislike, Mode=TwoWay}"  FontFamily="Marlett" FontSize="20" Content="6" Padding="0" BorderThickness="0" />
    </StackPanel>
</UserControl>
