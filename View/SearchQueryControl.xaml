<UserControl
    x:Class="Baconography.View.SearchQueryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Baconography.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xc="using:WinRTXamlToolkit.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContext="{Binding SearchQuery, Source={StaticResource Locator}}">

    <Grid>
        <StackPanel Orientation="Vertical">
            <xc:WatermarkTextBox Width="300" WatermarkText="Query" HorizontalAlignment="Left" Margin="0,5,0,5"
                                 Text="{Binding Query,Mode=TwoWay}" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Search"
                        Command="{Binding Search}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

